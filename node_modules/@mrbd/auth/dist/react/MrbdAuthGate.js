import { jsx as _jsx, Fragment as _Fragment } from "react/jsx-runtime";
import { useMrbdAuth } from "./context.js";
import { MrbdSignInScreen } from "./MrbdSignInScreen.js";
import { useMrbdAuthTheme } from "./theme.js";
/**
 * Gates its children behind an MRBD session. Shows the built-in sign-in screen
 * when signed out unless a custom `fallback` is provided.
 */
export function MrbdAuthGate({ children, loading, fallback, onSignedIn }) {
    const { status } = useMrbdAuth();
    if (status === "loading") {
        return _jsx(_Fragment, { children: loading ?? _jsx(DefaultLoading, {}) });
    }
    if (status === "signed-out") {
        return _jsx(_Fragment, { children: fallback ?? _jsx(MrbdSignInScreen, { onSignedIn: onSignedIn }) });
    }
    return _jsx(_Fragment, { children: children });
}
function DefaultLoading() {
    const theme = useMrbdAuthTheme();
    return (_jsx("div", { style: {
            display: "grid",
            placeItems: "center",
            height: "100%",
            color: theme.colorTextMuted,
            background: theme.colorBackground,
            fontFamily: theme.fontFamily,
            fontWeight: 700,
        }, children: "Loading\u2026" }));
}
//# sourceMappingURL=MrbdAuthGate.js.map