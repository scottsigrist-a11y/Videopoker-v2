import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useState } from "react";
import { useMrbdApproveDevice } from "./hooks.js";
import { useMrbdAuthStyles } from "./theme.js";
/**
 * Approve another device's sign-in from this already-signed-in device.
 *
 * The user types the short code shown on the other device (e.g. the glasses);
 * this calls `approveDevice`, and the other device then receives its own session
 * without entering an OTP. Render this inside an `MrbdAuthProvider` on a surface
 * that is already signed in.
 *
 * Need a different layout entirely? Build your own UI on {@link useMrbdApproveDevice}.
 */
export function MrbdApproveDeviceScreen({ onApproved, title, codeLength = 6, theme, className, style, }) {
    const { styles } = useMrbdAuthStyles(theme);
    const { phase, canApprove, error, approve } = useMrbdApproveDevice({ onApproved });
    const [code, setCode] = useState("");
    const submit = (event) => {
        event.preventDefault();
        void approve(code);
    };
    if (!canApprove) {
        return (_jsx("div", { className: className, style: { ...styles.container, ...style }, children: _jsx("p", { style: styles.hint, children: "Sign in on this device before approving another device." }) }));
    }
    return (_jsxs("div", { className: className, style: { ...styles.container, ...style }, children: [_jsxs("header", { style: { display: "flex", flexDirection: "column", gap: 6 }, children: [_jsx("p", { style: styles.label, children: "Approve a device" }), _jsx("h1", { style: styles.heading, children: title ?? "Sign in your other device" })] }), (phase === "idle" || phase === "approving" || phase === "error") && (_jsxs("form", { onSubmit: submit, style: { display: "flex", flexDirection: "column", gap: 16 }, children: [_jsx("p", { style: styles.hint, children: "Enter the code shown on the device you want to sign in." }), _jsx("input", { type: "text", inputMode: "text", autoComplete: "one-time-code", autoFocus: true, value: code, onChange: (event) => setCode(event.target.value), placeholder: "ABC-123", className: "mrbd-focusable", style: { ...styles.input, letterSpacing: "0.12em", textTransform: "uppercase", fontWeight: 800 }, "aria-label": "Device code", disabled: phase === "approving" }), _jsx("button", { type: "submit", className: "mrbd-focusable", style: styles.primaryButton, disabled: phase === "approving", children: phase === "approving" ? "Approving…" : "Approve" }), error && (_jsx("p", { style: styles.error, role: "alert", children: error }))] })), phase === "done" && _jsx("p", { style: styles.hint, children: "Approved. Your other device is signing in now." })] }));
}
//# sourceMappingURL=MrbdApproveDeviceScreen.js.map