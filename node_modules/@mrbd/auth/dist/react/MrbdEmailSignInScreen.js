import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { useState } from "react";
import { useMrbdEmailSignIn } from "./hooks.js";
import { MrbdOtpNumpad } from "./MrbdOtpNumpad.js";
import { useMrbdAuthStyles } from "./theme.js";
/**
 * Direct email-OTP sign-in for keyboard surfaces (a phone or desktop web app).
 *
 * Unlike {@link MrbdSignInScreen} (the glasses device-pairing flow), this screen
 * assumes the surface has its own keyboard: the user types their email, receives
 * a one-time code by email, and enters it here. The resulting session is bound to
 * the same `appId` and Supabase user as the glasses, so a user who signs in here
 * shares one identity (and one set of managed data) across devices.
 *
 * Need a different layout entirely? Build your own UI on {@link useMrbdEmailSignIn}.
 */
export function MrbdEmailSignInScreen({ onSignedIn, otpLength = 6, title, theme, className, style, }) {
    const { styles } = useMrbdAuthStyles(theme);
    const { phase, email, error, sendOtp, verifyOtp, resend } = useMrbdEmailSignIn({ onSignedIn });
    const [draftEmail, setDraftEmail] = useState("");
    const submitEmail = (event) => {
        event.preventDefault();
        void sendOtp(draftEmail);
    };
    return (_jsxs("div", { className: className, style: { ...styles.container, ...style }, children: [_jsxs("header", { style: { display: "flex", flexDirection: "column", gap: 6 }, children: [_jsx("p", { style: styles.label, children: "MRBD Sign in" }), _jsx("h1", { style: styles.heading, children: title ?? "Sign in to continue" })] }), phase === "email" && (_jsxs("form", { onSubmit: submitEmail, style: { display: "flex", flexDirection: "column", gap: 16 }, children: [_jsx("p", { style: styles.hint, children: "Enter your email and we'll send you a one-time code." }), _jsx("input", { type: "email", inputMode: "email", autoComplete: "email", autoFocus: true, value: draftEmail, onChange: (event) => setDraftEmail(event.target.value), placeholder: "you@example.com", className: "mrbd-focusable", style: styles.input, "aria-label": "Email address" }), _jsx("button", { type: "submit", className: "mrbd-focusable", style: styles.primaryButton, children: "Send code" })] })), phase === "sending_otp" && _jsx("p", { style: styles.hint, children: "Sending your code\u2026" }), phase === "otp" && (_jsxs("div", { style: { display: "flex", flexDirection: "column", gap: 12 }, children: [_jsxs("p", { style: styles.hint, children: ["Enter the ", otpLength, "-digit code we emailed to ", _jsx("strong", { children: email }), "."] }), _jsx(MrbdOtpNumpad, { length: otpLength, onSubmit: (token) => void verifyOtp(token), theme: theme }), _jsx("button", { type: "button", className: "mrbd-focusable", onClick: () => void resend(), style: styles.subtleButton, children: "Resend code" })] })), phase === "verifying" && _jsx("p", { style: styles.hint, children: "Verifying your code\u2026" }), phase === "done" && _jsx("p", { style: styles.hint, children: "You're signed in." }), error && (_jsx("p", { style: styles.error, role: "alert", children: error }))] }));
}
//# sourceMappingURL=MrbdEmailSignInScreen.js.map