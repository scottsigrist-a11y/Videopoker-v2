import { MrbdAuthError } from "./error.js";
export const DEFAULT_MRBD_AUTH_STORAGE_KEY = "mrbd.auth.session";
export function getDefaultAuthStorage() {
    if (typeof window === "undefined")
        return null;
    return window.localStorage ?? null;
}
export function readMrbdSession(storage, key) {
    if (!storage)
        return null;
    const raw = storage.getItem(key);
    if (!raw)
        return null;
    try {
        return JSON.parse(raw);
    }
    catch {
        storage.removeItem(key);
        return null;
    }
}
export function writeMrbdSession(storage, key, session) {
    if (!storage)
        return;
    try {
        storage.setItem(key, JSON.stringify(session));
    }
    catch (error) {
        throw new MrbdAuthError("storage_unavailable", "Unable to store the MRBD auth session.", {
            details: error,
        });
    }
}
export function removeMrbdSession(storage, key) {
    if (!storage)
        return;
    storage.removeItem(key);
}
//# sourceMappingURL=storage.js.map