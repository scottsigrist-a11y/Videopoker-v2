import { jsx as _jsx } from "react/jsx-runtime";
import { MRBD_MIN_TARGET_SIZE } from "@mrbd/core";
export function MrbdButton({ children, className, style, ...props }) {
    const buttonStyle = {
        minHeight: MRBD_MIN_TARGET_SIZE,
        borderRadius: 24,
        border: "2px solid transparent",
        background: "#1C1E21",
        color: "#ffffff",
        font: "inherit",
        fontWeight: 700,
        padding: "16px 24px",
        transition: "transform 300ms ease, border-color 300ms ease, box-shadow 300ms ease",
        ...style,
    };
    return (_jsx("button", { className: ["mrbd-focusable", className].filter(Boolean).join(" "), style: buttonStyle, ...props, children: children }));
}
//# sourceMappingURL=MrbdButton.js.map