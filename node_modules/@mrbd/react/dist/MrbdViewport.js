import { jsx as _jsx } from "react/jsx-runtime";
import { MRBD_SAFE_MARGIN, MRBD_VIEWPORT_SIZE } from "@mrbd/core";
export function MrbdViewport({ children, centered = false, className, style, ...props }) {
    const viewportStyle = {
        width: MRBD_VIEWPORT_SIZE.width,
        height: MRBD_VIEWPORT_SIZE.height,
        overflow: "hidden",
        boxSizing: "border-box",
        padding: MRBD_SAFE_MARGIN,
        background: "#000000",
        color: "#ffffff",
        display: centered ? "grid" : undefined,
        placeItems: centered ? "center" : undefined,
        ...style,
    };
    return (_jsx("div", { className: className, style: viewportStyle, ...props, children: children }));
}
//# sourceMappingURL=MrbdViewport.js.map