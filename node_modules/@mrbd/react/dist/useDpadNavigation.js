import { DPAD } from "@mrbd/core";
import { useEffect } from "react";
const DEFAULT_SELECTOR = [
    ".mrbd-focusable:not([disabled])",
    ".focusable:not([disabled])",
    "button:not([disabled])",
    "a[href]",
    "[tabindex]:not([tabindex='-1'])",
].join(",");
function isHTMLElement(element) {
    return element instanceof HTMLElement;
}
function getFocusableElements(selector) {
    return Array.from(document.querySelectorAll(selector)).filter((element) => isHTMLElement(element) && !element.hidden && element.offsetParent !== null);
}
function moveFocus(direction, selector, loop) {
    const focusables = getFocusableElements(selector);
    if (focusables.length === 0)
        return;
    const currentIndex = focusables.indexOf(document.activeElement);
    if (currentIndex === -1) {
        focusables[0]?.focus();
        return;
    }
    const delta = direction === "previous" ? -1 : 1;
    const nextIndex = currentIndex + delta;
    if (nextIndex < 0 || nextIndex >= focusables.length) {
        if (!loop)
            return;
        const wrapped = nextIndex < 0 ? focusables.length - 1 : 0;
        focusables[wrapped]?.focus();
        focusables[wrapped]?.scrollIntoView({ block: "nearest" });
        return;
    }
    focusables[nextIndex]?.focus();
    focusables[nextIndex]?.scrollIntoView({ block: "nearest" });
}
export function useDpadNavigation({ enabled = true, selector = DEFAULT_SELECTOR, loop = true, onBack, } = {}) {
    useEffect(() => {
        if (!enabled || typeof document === "undefined")
            return;
        const handleKeyDown = (event) => {
            switch (event.key) {
                case DPAD.UP:
                case DPAD.LEFT:
                    moveFocus("previous", selector, loop);
                    event.preventDefault();
                    break;
                case DPAD.DOWN:
                case DPAD.RIGHT:
                    moveFocus("next", selector, loop);
                    event.preventDefault();
                    break;
                case DPAD.SELECT:
                    if (document.activeElement instanceof HTMLElement) {
                        document.activeElement.click();
                        event.preventDefault();
                    }
                    break;
                case DPAD.BACK:
                    onBack?.();
                    if (onBack)
                        event.preventDefault();
                    break;
            }
        };
        document.addEventListener("keydown", handleKeyDown);
        return () => document.removeEventListener("keydown", handleKeyDown);
    }, [enabled, loop, onBack, selector]);
}
//# sourceMappingURL=useDpadNavigation.js.map