import { getCurrentMrbdPosition } from "@mrbd/core";
import { useCallback, useState } from "react";
export function useMrbdLocation() {
    const [result, setResult] = useState(null);
    const [loading, setLoading] = useState(false);
    const getCurrentPosition = useCallback(async (options) => {
        setLoading(true);
        const nextResult = await getCurrentMrbdPosition(options);
        setResult(nextResult);
        setLoading(false);
        return nextResult;
    }, []);
    return { result, loading, getCurrentPosition };
}
//# sourceMappingURL=useMrbdLocation.js.map